const amount = 3

const evaluate = amount => {
    if (amount > 10) {
        console.log(`large number`);
    } else {
        console.log(`small number`);
    }
}
 
evaluate(amount)

console.log(`This is an intro to node and express.js framework`);